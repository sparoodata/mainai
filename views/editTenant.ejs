<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Tenant</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    h1 { color: #333; }
    form { margin-top: 20px; }
    label, input, select { display: block; margin: 10px 0; }
    input, select { padding: 8px; width: 200px; }
    button { padding: 10px; background-color: #007bff; color: white; border: none; cursor: pointer; }
    button:hover { background-color: #0056b3; }
  </style>
</head>
<body>
  <h1>Edit Tenant: <%= tenant.name %></h1>
  <form action="/edittenant/<%= id %>" method="POST" enctype="multipart/form-data">
    <input type="hidden" name="tenantId" value="<%= tenant._id %>">

    <label for="name">Name:</label>
    <input type="text" name="name" id="name" value="<%= tenant.name %>" required>

    <label for="propertyName">Property Name:</label>
    <select name="propertyName" id="propertyName" required>
      <option value="">-- Select Property --</option>
      <% properties.forEach(property => { %>
        <option value="<%= property.name %>" <%= tenant.propertyName === property.name ? 'selected' : '' %>>
          <%= property.name %>
        </option>
      <% }) %>
    </select>

    <label for="unitAssigned">Unit Assigned:</label>
    <select name="unitAssigned" id="unitAssigned" required>
      <option value="">-- Select Unit --</option>
      <% units.forEach(unit => { %>
        <option value="<%= unit._id %>" <%= tenant.unitAssigned.toString() === unit._id.toString() ? 'selected' : '' %>>
          <%= unit.unitNumber %> (<%= unit.unit_id %>)
        </option>
      <% }) %>
    </select>

    <label for="lease_start">Lease Start Date:</label>
    <input type="date" name="lease_start" id="lease_start" value="<%= tenant.lease_start.toISOString().split('T')[0] %>" required>

    <label for="deposit">Deposit:</label>
    <input type="number" name="deposit" id="deposit" value="<%= tenant.deposit %>" required>

    <label for="rent_amount">Rent Amount:</label>
    <input type="number" name="rent_amount" id="rent_amount" value="<%= tenant.rent_amount %>" required>

    <label for="photo">Update Photo:</label>
    <input type="file" name="photo" id="photo" accept="image/*">

    <label for="idProof">Update ID Proof:</label>
    <input type="file" name="idProof" id="idProof" accept="image/*">

    <button type="submit">Update Tenant</button>
  </form>
</body>
</html>